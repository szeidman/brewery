<h1><%= @ingredient.name -%></h1>
<h3>(<%= @ingredient.kind -%>)</h3>
<br>
<%= render partial: 'attribute', locals: {title: "Origin:", value: @ingredient.origin} %>
<%= render partial: 'amount' %>
<br><br>
<%= render partial: 'found' %>

<br>
<%= button_to "Edit Ingredient", edit_ingredient_path, method: :get if current_user %>
<%= button_to "Delete Ingredient", ingredient_path(@ingredient), method: :delete if current_user%>
